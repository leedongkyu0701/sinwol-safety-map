# Published Data

검증을 통과한 브라우저용 정적 JSON을 이 디렉터리에 생성한다. Fire Water와 Shelter ETL은 `fire-water.json`, `shelters.json`을 Publish하며, AED ETL은 이동형 후보의 사람 검토가 완료된 뒤 `aeds.json`을 Publish한다. Source별 요약은 `metadata.json`에서 관리한다.
