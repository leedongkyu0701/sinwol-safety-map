import { SafetyMap } from "@/features/safety-map/components/safety-map";

export default function HomePage() {
  return (
    <main className="h-dvh w-full overflow-hidden">
      <SafetyMap />
    </main>
  );
}
